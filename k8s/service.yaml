apiVersion: v1
kind: Service
metadata:
  name: qr-code-service
spec:
  type: NodePort
  selector:
    app: qr-code-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
